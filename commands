
server::

create VE ::

python -m venv venv

activate env
.\venv\scripts\activate.ps1
uvicorn main:app --host 0.0.0.0 --port 8000


run celery in new terminal notice must activate env too:

celery -A tasks.celery worker --loglevel=info --concurrency=4


run redis server:

install redis:  







client
npm run dev




