You are a senior QA engineer specializing in end-to-end testing.
Generate a complete E2E test suite for a WhatsApp-style chat application
using Playwright with TypeScript.

My stack:
- Frontend: React + TypeScript + Tailwind CSS (runs on http://localhost:5173)
- Backend: FastAPI + WebSocket (runs on http://localhost:8000)
- Database: PostgreSQL
- Cache/PubSub: Redis
- Auth: JWT (access token + refresh token)

The full system must be running during these tests.
Use Playwright's built-in WebSocket interception where needed.

---

## Test environment setup

Before all tests:
- Seed database with test users:
    user_a: { email: "user_a@test.com", password: "Test1234!" }
    user_b: { email: "user_b@test.com", password: "Test1234!" }
    user_c: { email: "user_c@test.com", password: "Test1234!" }
- Clear Redis before each test
- Each test gets a fresh browser context (no shared cookies/storage)
- Run tests in headed mode for debugging, headless for CI

Create these Playwright fixtures:
    - loggedInPage: page already authenticated as user_a
    - twoUserPages: two browser contexts, one per user
    - chatPage: page with an active chat already open

---

## 1. Authentication flows

test: user can register a new account
- Navigate to /register
- Fill name, email, password fields
- Submit form
- Assert redirected to chat page
- Assert sidebar visible

test: user can log in with correct credentials
- Navigate to /login
- Fill email + password
- Submit
- Assert redirected to chat page
- Assert user name visible in UI

test: login fails with wrong password
- Navigate to /login
- Fill correct email, wrong password
- Submit
- Assert still on /login
- Assert error message visible ("Invalid credentials" or similar)
- Assert no token stored

test: login fails with non-existent email
- Same as above with unknown email
- Assert error message visible

test: user is redirected to /login when accessing / without token
- Fresh context, navigate to /
- Assert redirected to /login

test: user stays logged in on page refresh
- Log in
- Refresh page (F5)
- Assert still on chat page, not redirected to login

test: token refresh works transparently
- Log in
- Manually expire access token in memory (via page.evaluate)
- Trigger an API call (send a message)
- Assert token silently refreshed
- Assert message sent successfully, no login redirect

test: logging out clears session
- Log in
- Click logout button
- Assert redirected to /login
- Assert token cleared from memory
- Navigate back to / directly
- Assert redirected to /login again

test: two sessions same user
- Log in as user_a in browser context 1
- Log in as user_a in browser context 2
- Both sessions active simultaneously
- Message sent from context 1 appears in context 2

---

## 2. Layout & navigation

test: sidebar and chat window both visible on desktop
- Log in
- Assert sidebar visible (left, 340px wide)
- Assert chat window visible on right
- Assert no horizontal scrollbar

test: mobile shows sidebar only by default
- Set viewport to 390x844 (iPhone 14)
- Log in
- Assert sidebar visible full screen
- Assert chat window NOT visible

test: mobile: selecting chat shows chat window
- Mobile viewport
- Log in
- Click first chat in sidebar
- Assert chat window visible full screen
- Assert sidebar NOT visible

test: mobile: back button returns to sidebar
- Mobile viewport
- Open a chat
- Click back button in chat header
- Assert sidebar visible
- Assert chat window NOT visible

test: dark mode applied when system preference is dark
- Set colorScheme: 'dark' in browser context
- Log in
- Assert sidebar background is dark (#111B21)
- Assert message area background is dark (#0B141A)
- Assert text color is light

test: no layout overflow on any viewport
- Test viewports: 375px, 768px, 1280px, 1920px
- For each: log in, open a chat, send a message
- Assert no horizontal scroll at any viewport
- Assert no element overflows its container

---

## 3. Chat list (sidebar)

test: sidebar shows all chats on load
- Log in as user_a (has 3 seeded chats)
- Assert 3 chat items visible in sidebar
- Assert each shows correct contact name

test: chat list shows last message preview
- Seed: user_a has sent "Hello there" to user_b
- Log in as user_a
- Assert "Hello there" visible in user_b's chat list item

test: chat list shows correct timestamp
- Seed: message sent 2 minutes ago
- Log in
- Assert timestamp shows relative time ("2m" or "just now")

test: unread badge shows correct count
- Seed: user_b sent 3 messages to user_a, user_a has not read them
- Log in as user_a
- Assert badge on user_b chat shows "3"

test: unread badge disappears when chat opened
- Same seed as above
- Click user_b's chat
- Assert badge disappears

test: search filters chat list
- Log in (3 contacts: Alice, Bob, Charlie)
- Type "ali" in search input
- Assert only Alice's chat visible
- Assert Bob and Charlie hidden

test: search is case insensitive
- Type "ALICE" in search
- Assert Alice's chat visible

test: clearing search restores full list
- Type "ali", assert 1 result
- Clear input
- Assert all 3 chats visible

test: search with no results shows empty state
- Type "zzzzz"
- Assert "No chats found" message visible
- Assert zero chat items

test: chat list reorders when new message arrives
- Log in (Alice is first, Bob is second)
- Incoming message arrives from Bob via WebSocket
- Assert Bob's chat moves to top of list

---

## 4. Messaging — sending

test: user can send a message
- Open chat with user_b
- Type "Hello from E2E test" in input
- Press Enter
- Assert message appears in message list on right side
- Assert message text is "Hello from E2E test"
- Assert input is cleared after send

test: send button replaces mic when typing
- Open a chat
- Assert microphone icon visible (input empty)
- Type one character
- Assert send button visible (green circle)
- Clear input
- Assert microphone icon visible again

test: clicking send button sends message
- Type "Test message"
- Click send button (not Enter)
- Assert message appears in list

test: pressing Enter sends message
- Type "Enter test"
- Press Enter key
- Assert message appears

test: empty input does not send
- Open chat, input is empty
- Press Enter
- Assert no new message in list
- Assert input still empty

test: whitespace-only input does not send
- Type "     " (spaces only)
- Press Enter
- Assert no new message appears

test: very long message sends correctly
- Type a 1000-character string
- Send
- Assert full message visible in bubble
- Assert bubble does not overflow chat window width

test: sending multiple messages rapidly
- Send 5 messages one after another quickly
- Assert all 5 appear in correct order
- Assert all 5 on right side

test: sidebar updates after sending
- Open chat with user_b
- Send "Updated preview"
- Assert sidebar shows "Updated preview" as last message for user_b
- Assert timestamp updated

---

## 5. Messaging — receiving

test: message received from other user appears in real time
- Open two browser contexts: user_a and user_b
- user_a opens chat with user_b
- user_b sends "Hi from user_b" via their chat window
- Assert message appears in user_a's chat window without refresh
- Assert message on left side (received)

test: received message updates sidebar
- user_a is on a DIFFERENT chat (not user_b's)
- user_b sends a message to user_a
- Assert user_b's chat item moves to top of user_a's sidebar
- Assert last message preview shows user_b's message
- Assert unread badge increments

test: sender does NOT see their own message twice
- user_a sends a message
- Assert message appears exactly once in list

test: message delivered to all members in group chat
- Three browser contexts: user_a, user_b, user_c all in same group chat
- user_a sends "Group message"
- Assert "Group message" appears in user_b's chat window
- Assert "Group message" appears in user_c's chat window

test: messages arrive in correct order under load
- user_b sends 10 messages rapidly to user_a
- user_a's chat window shows all 10 in correct order

test: incoming message while scrolled up shows floating button
- Seed chat with 50 messages (to enable scrolling)
- Open chat, scroll up to top
- user_b sends a new message
- Assert "1 new message ↓" floating button appears
- Click it
- Assert scrolled to bottom
- Assert new message visible
- Assert floating button disappears

---

## 6. WebSocket connection

test: connection indicator shows "connected" on load
- Log in
- Assert UI shows connected state (green dot or no disconnection banner)

test: disconnection banner appears when WebSocket drops
- Log in
- Intercept and close WebSocket connection via Playwright
- Assert "Connecting..." or disconnection indicator visible in UI

test: UI recovers after WebSocket reconnects
- Drop WebSocket connection
- Wait for reconnection (max 5 seconds)
- Assert connected indicator returns
- Assert user can send message successfully

test: messages sent during disconnection are handled
- Drop WebSocket
- User types and tries to send a message
- Assert either: message queued and sent on reconnect,
  OR: user shown clear error that message was not sent
- Assert no silent failure

test: multiple rapid reconnects do not cause duplicate messages
- Force disconnect/reconnect 3 times quickly
- user_b sends a message after reconnect
- Assert message appears exactly once in user_a's chat

---

## 7. Read receipts & message status

test: sent message shows single tick initially
- Send a message
- Assert single gray tick visible on bubble

test: double tick appears when delivered
- Send a message
- user_b receives it (WebSocket delivers)
- Assert double gray tick on user_a's bubble

test: blue ticks appear when recipient reads message
- user_b opens the chat
- Assert double BLUE tick on user_a's bubble

---

## 8. Scroll behavior

test: chat opens scrolled to bottom
- Open chat with 30 messages
- Assert last message visible without scrolling

test: new message auto-scrolls to bottom
- Open chat, verify at bottom
- user_b sends message
- Assert still at bottom, new message visible

test: no auto-scroll when user has scrolled up
- Open chat, scroll to top
- user_b sends message
- Assert user remains at scrolled-up position
- Assert floating "new message" button visible

test: manual scroll to bottom works
- Scroll to top
- Click "scroll to bottom" button
- Assert scrolled to bottom
- Assert latest message visible

---

## 9. Error & edge case flows

test: chat with no messages shows empty state
- Seed: user_a and user_b have a chat but zero messages
- Open that chat
- Assert empty state message visible
  ("No messages yet. Say hello!")
- Assert no errors in console

test: very long contact name truncated in sidebar
- Seed: contact with 100-character name
- Assert name truncated with ellipsis in sidebar
- Assert does not break sidebar layout

test: avatar fallback when image fails to load
- Seed: contact with broken avatar URL
- Assert initials shown instead of broken image icon

test: app handles server returning 500 error
- Intercept API call, return 500
- Assert error state shown to user
- Assert app does not crash (white screen)

test: app handles server returning 401 after token expiry
- Intercept API call, return 401
- Assert user redirected to /login
- Assert not stuck in redirect loop

test: app handles network going offline
- Log in, open a chat
- Set browser to offline mode (page.context().setOffline(true))
- Try to send a message
- Assert user sees error or message marked as failed
- Set browser back online
- Assert app recovers

---

## 10. Performance E2E checks

test: initial page load under 2 seconds
- Navigate to app
- Assert page fully interactive in under 2000ms
- Use Playwright's performance API to measure

test: chat with 200 messages renders without freeze
- Seed: 200 messages in one chat
- Open that chat
- Assert page responsive within 500ms
- Assert no layout jank (use page.evaluate to check long tasks)

test: search filters 100 chats in under 100ms
- Seed: 100 chats
- Type in search
- Assert results update within 100ms

test: 10 messages received in 1 second all appear
- user_b sends 10 messages within 1 second
- Assert all 10 visible in user_a's chat within 2 seconds

---

## Page Object Model

Create these Page Objects:

LoginPage:
  - goto()
  - fillEmail(email)
  - fillPassword(password)
  - submit()
  - getErrorMessage()

ChatPage:
  - getSidebarItems()
  - selectChat(contactName)
  - getSearchInput()
  - search(query)
  - getUnreadBadge(contactName)
  - sendMessage(text)
  - getMessages()
  - getLastMessage()
  - getConnectionStatus()
  - scrollToTop()
  - getNewMessageButton()
  - clickNewMessageButton()

MessageBubble:
  - getText()
  - getTimestamp()
  - getReadReceipt()
  - isSent()
  - isReceived()

---

## Configuration

playwright.config.ts:
- baseURL: http://localhost:5173
- Two projects: chromium and mobile-chrome
- Mobile chrome viewport: 390x844
- Retries: 2 on CI, 0 locally
- Reporter: html (always), line (CI)
- globalSetup: seed database + start dev server
- globalTeardown: clean database
- Timeout per test: 30 seconds
- Use test.describe.parallel where tests are independent

---

## Fixtures file

Create fixtures.ts with:
- loggedInPage: authenticates user_a, returns page
- twoUserPages: returns { pageA, pageB } both logged in
- chatPage: logged in + first chat open
- mobilePage: logged in with mobile viewport

---

## Helper utilities

Create helpers.ts with:
- seedChat(userA, userB, messages[]) — seeds messages via API
- clearAllChats() — cleans up after test
- waitForMessage(page, text) — waits for specific text in message list
- dropWebSocket(page) — intercepts and closes WS connection
- getMessageCount(page) — returns number of bubbles in message list
- assertNoDuplicateMessages(page) — asserts no two bubbles have same id

---

Deliver in this order:
1. playwright.config.ts
2. fixtures.ts
3. helpers.ts
4. tests/auth.spec.ts
5. tests/layout.spec.ts
6. tests/sidebar.spec.ts
7. tests/messaging.spec.ts
8. tests/websocket.spec.ts
9. tests/edge-cases.spec.ts
10. tests/performance.spec.ts
11. pages/LoginPage.ts
12. pages/ChatPage.ts

After all code provide:
- Exact npm install command for all dependencies
- How to run: single test, single file, full suite, CI mode
- How to view HTML report